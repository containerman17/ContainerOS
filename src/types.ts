export type AppUpdate = {
    image: string,
    internetPort: number,
    scale: number,
    hardCpuLimit: number,
    hardMemoryLimit: number,
    internetDomain: string,
    namespace: string,
    name: string
}
